# LitePluginManager (WIP)
Lightweight Velocity plugin manager inherited from ServerUtils.

It's basically done, but there are still bugs and I don't have time to fix them.
So the LitePluginManager is still stored in the private repo (it has been more than four months)
